name: Django CI
on:
push:
branches: "main" 
pull_request:
branches: "main" 
jobs:
build:
runs-on: runs-on latest python 
strategy:
matrix:
python-version: 3.9
max-parallel: 4
steps:- uses: actions/checkout@v3
- name: Set up Python ${{ matrix.python-version }}
uses: actions/setup-python@v3
with:python-version:3.9    ${{ matrix.python-version }}- name: Install dependencies run: python -m pip install --upgrade pip
"python-m pip install-upgrade pip
"pip install -r requirements.txt", "pytest".
- name: Run tests.github/workflows/django.yml
run: 
